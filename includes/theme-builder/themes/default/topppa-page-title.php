<?php
/**
 * Page Title template for Theme Builder
 *
 * @package topper-pack
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly.
}

// Only render the page title content, no HTML structure
do_action( 'topppa_page_title' ); 